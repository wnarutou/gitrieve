# 移除 os.Chdir 并发 cwd 竞态实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 消除并发 job 下 `os.Chdir` 导致的进程级 cwd 竞态——归档改用绝对路径 `gitDir`，不再修改进程 cwd，并加并发回归测试。

**Architecture:** 新增单职责包 `internal/archive`，提供 `Create(ctx, sourceDir, targetName)` 用 `mholt/archives` 从绝对路径打包 gzip tarball（永不 chdir）。三处调用点（repository/issue/discussion）删除 `os.Chdir(path.Dir(gitDir))` → `FilesFromDisk(相对路径)` → `os.Chdir(currentDir)` 整段，改为 `archive.Create(ctx, gitDir, target)`。归档条目布局与旧相对 key 输出完全一致（`target/<rel>`）。

**Tech Stack:** Go 1.x、`github.com/mholt/archives v0.1.1`、标准库 `archive/tar` + `compress/gzip`（测试解包）、`testify`（断言）。

**设计文档:** `docs/superpowers/specs/2026-08-10-cwd-race-design.md`

## Global Constraints

- **归档条目布局必须保持 `target/<rel>`**（与旧相对路径 key 输出字节级一致），不可改变归档内部结构。
- **`internal/{repository,issue,discussion}` 三包内不得再出现 `os.Chdir`**（全仓 `os.Chdir` 出现次数归零）。
- `archive.Create` 的 `sourceDir` 必须传**绝对路径**（gitDir），不得传相对路径。
- 共享 helper 用调用方传入的 `context.Context`（不是 `context.TODO()`），job 取消时更早中止遍历。
- 验收：`go build ./...`、`go test ./...`、`go test -race ./internal/archive/` 全绿。
- **范围外（不做）**：「同仓库同组件并发写同一 `.git`」的 per-repo 锁，记为独立 follow-up。
- 现有测试均为离线单测（取消 ctx 早退），`go test ./internal/...` 可离线运行。

---

### Task 1: 新建 `internal/archive` 包与 `Create` helper（TDD）

**Files:**
- Create: `internal/archive/archive.go`
- Create: `internal/archive/archive_test.go`

**Interfaces:**
- Produces: `archive.Create(ctx context.Context, sourceDir, targetName string) (*bytes.Buffer, error)` — 将绝对路径 `sourceDir` 打包为 gzip tarball，`targetName` 作为归档内根目录；永不修改进程 cwd。
- Produces（测试内 helper，后续 Task 也用）: `extract(t *testing.T, buf *bytes.Buffer) map[string]string` — 用标准库解包 gzip tar，返回「常规文件归档名 → 内容」映射，跳过目录条目。

- [ ] **Step 1: 写失败测试（条目名兼容性）**

`internal/archive/archive_test.go`：

```go
package archive

import (
	"archive/tar"
	"bytes"
	"compress/gzip"
	"context"
	"io"
	"os"
	"path"
	"testing"

	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

// extract 将 gzip tarball 解包为「常规文件归档名 → 内容」映射，跳过目录条目。
func extract(t *testing.T, buf *bytes.Buffer) map[string]string {
	t.Helper()
	gzr, err := gzip.NewReader(bytes.NewReader(buf.Bytes()))
	require.NoError(t, err)
	defer gzr.Close()

	tr := tar.NewReader(gzr)
	contents := make(map[string]string)
	for {
		hdr, err := tr.Next()
		if err == io.EOF {
			break
		}
		require.NoError(t, err)
		if hdr.Typeflag != tar.TypeReg {
			continue
		}
		data, err := io.ReadAll(tr)
		require.NoError(t, err)
		contents[hdr.Name] = string(data)
	}
	return contents
}

// TestCreateArchiveEntryNamesMatchRelativeLayout 锁定：用绝对源目录 Create 的
// 归档条目布局与旧相对路径写法一致（<target>/<rel>），防止 archives 升级破坏兼容。
func TestCreateArchiveEntryNamesMatchRelativeLayout(t *testing.T) {
	src := path.Join(t.TempDir(), "tree")
	require.NoError(t, os.MkdirAll(path.Join(src, "sub"), 0o755))
	require.NoError(t, os.WriteFile(path.Join(src, "top.txt"), []byte("top"), 0o644))
	require.NoError(t, os.WriteFile(path.Join(src, "sub", "inner.txt"), []byte("inner"), 0o644))

	buf, err := Create(context.Background(), src, "target")
	require.NoError(t, err)

	files := extract(t, buf)
	assert.Equal(t, "top", files["target/top.txt"])
	assert.Equal(t, "inner", files["target/sub/inner.txt"])
}
```

- [ ] **Step 2: 运行确认失败**

Run: `go test ./internal/archive/ -run TestCreateArchiveEntryNamesMatchRelativeLayout -v`
Expected: FAIL（编译失败：`internal/archive` 包不存在 / `Create` undefined）

- [ ] **Step 3: 最小实现**

`internal/archive/archive.go`：

```go
package archive

import (
	"bytes"
	"context"

	"github.com/mholt/archives"
)

// Create 将绝对路径 sourceDir 的内容打包为 gzip tarball，targetName 作为归档
// 内的根目录。它永不修改进程 cwd，因此可被并发 job goroutine 安全调用。
//
// 注意：sourceDir 必须是绝对路径，否则打包结果取决于进程当前目录。
// TODO: 超过一定大小的归档先写临时文件再压缩，避免整个仓库常驻内存（旧实现
// 中该 TODO 挂在调用方，现随打包逻辑一并迁到此处）。
func Create(ctx context.Context, sourceDir, targetName string) (*bytes.Buffer, error) {
	files, err := archives.FilesFromDisk(ctx, &archives.FromDiskOptions{}, map[string]string{
		sourceDir: targetName,
	})
	if err != nil {
		return nil, err
	}

	buf := &bytes.Buffer{}
	format := archives.CompressedArchive{
		Compression: archives.Gz{},
		Archival:    archives.Tar{},
	}
	if err := format.Archive(ctx, buf, files); err != nil {
		return nil, err
	}
	return buf, nil
}
```

- [ ] **Step 4: 运行确认通过**

Run: `go test ./internal/archive/ -run TestCreateArchiveEntryNamesMatchRelativeLayout -v`
Expected: PASS（`target/top.txt` 与 `target/sub/inner.txt` 断言通过）

- [ ] **Step 5: 提交**

```bash
git add internal/archive/archive.go internal/archive/archive_test.go
git commit -m "feat: add concurrency-safe archive.Create helper using absolute paths"
```

---

### Task 2: 并发隔离回归测试

**Files:**
- Modify: `internal/archive/archive_test.go`

**Interfaces:**
- Consumes: `Create(ctx, sourceDir, targetName)`、`extract(t, buf)`（均来自 Task 1）。
- Produces: 无新导出。

- [ ] **Step 1: 追加并发测试**

在 `internal/archive/archive_test.go` 末尾追加（补 imports：`fmt`、`sync`）：

```go
// TestCreateArchiveConcurrentIsolation 从多个 goroutine 同时 Create，断言每个
// 归档只含自己的 sentinel 内容、且进程 cwd 未被改动。这是旧 os.Chdir 实现的
// 回归护栏：旧实现并发时相互踩踏 cwd，归档会串到别的仓库目录。
func TestCreateArchiveConcurrentIsolation(t *testing.T) {
	base := t.TempDir()
	const n = 12

	cwdBefore, err := os.Getwd()
	require.NoError(t, err)

	dirs := make([]string, n)
	for i := 0; i < n; i++ {
		src := path.Join(base, fmt.Sprintf("repo%d", i), "code")
		require.NoError(t, os.MkdirAll(src, 0o755))
		require.NoError(t, os.WriteFile(path.Join(src, "file.txt"), []byte(fmt.Sprintf("sentinel-%d", i)), 0o644))
		dirs[i] = src
	}

	start := make(chan struct{})
	bufs := make([]*bytes.Buffer, n)
	errs := make([]error, n)
	var wg sync.WaitGroup
	for i := 0; i < n; i++ {
		wg.Add(1)
		go func(i int) {
			defer wg.Done()
			<-start
			bufs[i], errs[i] = Create(context.Background(), dirs[i], "code")
		}(i)
	}
	close(start)
	wg.Wait()

	for i := 0; i < n; i++ {
		require.NoError(t, errs[i])
		want := map[string]string{"code/file.txt": fmt.Sprintf("sentinel-%d", i)}
		assert.Equal(t, want, extract(t, bufs[i]), "archive %d must contain exactly its own sentinel", i)
	}

	cwdAfter, err := os.Getwd()
	require.NoError(t, err)
	assert.Equal(t, cwdBefore, cwdAfter, "Create must never change the process cwd")
}
```

- [ ] **Step 2: 运行确认通过（含 -race）**

Run: `go test -race ./internal/archive/ -v`
Expected: PASS（helper 天然安全，此测试到即绿；`-race` 同时守住周围共享内存安全）

- [ ] **Step 3: 提交**

```bash
git add internal/archive/archive_test.go
git commit -m "test: add concurrent archive isolation regression test"
```

---

### Task 3: `repository.go`（code + wiki 模式）改用 `archive.Create`

**Files:**
- Modify: `internal/repository/repository.go:1-21`（imports）、`internal/repository/repository.go:327-397`（归档块）
- Test: `internal/repository/repository_test.go`（已有，离线）

**Interfaces:**
- Consumes: `archive.Create(ctx, sourceDir, targetName)`（Task 1）。
- Produces: 无签名变更。`repository.Sync(ctx, repo, iswiki, storages)` 保持不变。

- [ ] **Step 1: 改 imports**

删除 `"bytes"` 与 `"github.com/mholt/archives"`，新增 `"github.com/wnarutou/gitrieve/internal/archive"`（置于 `github.com/wnarutou/gitrieve/internal/config` 之前）。`context`、`os`、`path`、`time` 保留。

- [ ] **Step 2: 替换归档块**

把以下整段（含两个 `os.Chdir`）：

```go
		// change directory to the parent directory of the repo
		err = os.Chdir(path.Dir(gitDir))
		if err != nil {
			ui.Errorf("Error changing directory, %s", err)
		}

		var sourceDir string
		var targetDir string
		if iswiki {
			sourceDir = "wiki"
			targetDir = r.Name + "_wiki"
		} else {
			sourceDir = "code"
			targetDir = r.Name
		}
		files, err := archives.FilesFromDisk(context.TODO(), &archives.FromDiskOptions{}, map[string]string{
			sourceDir: targetDir,
		})
		if err != nil {
			ui.Errorf("Error reading files, %s", err)
			return err
		}

		// For codes, there is no need to save the history.
		// The latest one is the full version and already contains all the history,
		// so it can be replaced directly.
		// now := time.Now().Format("20060102150405")
		base := targetDir + ".tar.gz"
		// TODO store to a temporary file first if greater than certain size,
		//      we can use isUpdated to support this feature temporality
		archive := &bytes.Buffer{}

		format := archives.CompressedArchive{
			Compression: archives.Gz{},
			Archival:    archives.Tar{},
		}
		err = format.Archive(context.Background(), archive, files)
		if err != nil {
			ui.Errorf("Error creating archive, %s", err)
			return err
		}

		// change to current dir
		err = os.Chdir(currentDir)
		if err != nil {
			ui.Errorf("Error changing directory, %s", err)
		}
```

替换为：

```go
		var targetDir string
		if iswiki {
			targetDir = r.Name + "_wiki"
		} else {
			targetDir = r.Name
		}

		// Archive the working tree directly from gitDir. Create takes an
		// absolute path and never changes the process cwd, so it is safe to
		// run from concurrent job goroutines.
		buf, err := archive.Create(syncCtx, gitDir, targetDir)
		if err != nil {
			ui.Errorf("Error creating archive, %s", err)
			return err
		}

		// For codes, there is no need to save the history: the latest one is
		// the full version and already contains all the history, so it can be
		// replaced directly.
		base := targetDir + ".tar.gz"
```

- [ ] **Step 3: 更新存储循环中的变量引用**

存储循环内 `backend.PutObject(..., archive.Bytes())` 改为 `buf.Bytes()`。

- [ ] **Step 4: 验证**

Run: `go build ./...`
Run: `go test ./internal/repository/ -v`
Run: `grep -n "os.Chdir" internal/repository/repository.go` → 无输出
Expected: build 通过、repository 包测试 PASS、无 `os.Chdir`

- [ ] **Step 5: 提交**

```bash
git add internal/repository/repository.go
git commit -m "fix: archive repo/wiki via absolute gitDir without os.Chdir"
```

---

### Task 4: `issue.go` 改用 `archive.Create`

**Files:**
- Modify: `internal/issue/issue.go:1-20`（imports）、`internal/issue/issue.go:226-258`（归档块）
- Test: `internal/issue/issue_test.go`（已有，离线）

**Interfaces:**
- Consumes: `archive.Create(ctx, sourceDir, targetName)`（Task 1）。
- Produces: 无签名变更。`issue.Sync(ctx, repo, storages)` 保持不变。

- [ ] **Step 1: 改 imports**

删除 `"bytes"` 与 `"github.com/mholt/archives"`，新增 `"github.com/wnarutou/gitrieve/internal/archive"`（置于 `github.com/wnarutou/gitrieve/internal/config` 之前）。`context`、`os`、`path` 等保留。

- [ ] **Step 2: 替换归档块**

把以下整段（含两个 `os.Chdir`）：

```go
	if isUpdated {
		// Change directory to the parent directory of the repo
		err = os.Chdir(path.Dir(gitDir))
		if err != nil {
			ui.Errorf("Error changing directory, %s", err)
		}

		files, err := archives.FilesFromDisk(context.TODO(), &archives.FromDiskOptions{}, map[string]string{
			"issues": "issues",
		})
		if err != nil {
			ui.Errorf("Error reading files, %s", err)
			return err
		}

		base := "issues.tar.gz"
		archive := &bytes.Buffer{}

		format := archives.CompressedArchive{
			Compression: archives.Gz{},
			Archival:    archives.Tar{},
		}
		err = format.Archive(context.Background(), archive, files)
		if err != nil {
			ui.Errorf("Error creating archive, %s", err)
			return err
		}

		// change to current dir
		err = os.Chdir(currentDir)
		if err != nil {
			ui.Errorf("Error changing directory, %s", err)
		}
```

替换为：

```go
	if isUpdated {
		// Archive the issues dir directly from gitDir. Create takes an
		// absolute path and never changes the process cwd, so it is safe to
		// run from concurrent job goroutines.
		buf, err := archive.Create(ctx, gitDir, "issues")
		if err != nil {
			ui.Errorf("Error creating archive, %s", err)
			return err
		}

		base := "issues.tar.gz"
```

- [ ] **Step 3: 更新存储循环中的变量引用**

存储循环内 `backend.PutObject(..., archive.Bytes())` 改为 `buf.Bytes()`。

- [ ] **Step 4: 验证**

Run: `go build ./...`
Run: `go test ./internal/issue/ -v`
Run: `grep -n "os.Chdir" internal/issue/issue.go` → 无输出
Expected: build 通过、issue 包测试 PASS、无 `os.Chdir`

- [ ] **Step 5: 提交**

```bash
git add internal/issue/issue.go
git commit -m "fix: archive issues via absolute gitDir without os.Chdir"
```

---

### Task 5: `discussion.go` 改用 `archive.Create`

**Files:**
- Modify: `internal/discussion/discussion.go:1-21`（imports）、`internal/discussion/discussion.go:450-481`（归档块）
- Test: `internal/discussion/discussion_test.go`（已有，离线）

**Interfaces:**
- Consumes: `archive.Create(ctx, sourceDir, targetName)`（Task 1）。
- Produces: 无签名变更。`discussion.Sync(ctx, repo, storages)` 保持不变。

- [ ] **Step 1: 改 imports**

删除 `"bytes"` 与 `"github.com/mholt/archives"`，新增 `"github.com/wnarutou/gitrieve/internal/archive"`（置于 `github.com/wnarutou/gitrieve/internal/config` 之前）。`context`、`os`、`path` 等保留。

- [ ] **Step 2: 替换归档块**

把以下整段（含两个 `os.Chdir`）：

```go
	if isUpdated {
		err = os.Chdir(path.Dir(gitDir))
		if err != nil {
			ui.Errorf("Error changing directory: %s", err)
		}

		files, err := archives.FilesFromDisk(context.TODO(), &archives.FromDiskOptions{}, map[string]string{
			"discussion": "discussion",
		})
		if err != nil {
			ui.Errorf("Error reading files: %s", err)
			return err
		}

		base := "discussions.tar.gz"
		archive := &bytes.Buffer{}

		format := archives.CompressedArchive{
			Compression: archives.Gz{},
			Archival:    archives.Tar{},
		}
		err = format.Archive(context.Background(), archive, files)
		if err != nil {
			ui.Errorf("Error creating archive: %s", err)
			return err
		}

		err = os.Chdir(currentDir)
		if err != nil {
			ui.Errorf("Error changing directory: %s", err)
		}
```

替换为：

```go
	if isUpdated {
		// Archive the discussion dir directly from gitDir. Create takes an
		// absolute path and never changes the process cwd, so it is safe to
		// run from concurrent job goroutines.
		buf, err := archive.Create(ctx, gitDir, "discussion")
		if err != nil {
			ui.Errorf("Error creating archive: %s", err)
			return err
		}

		base := "discussions.tar.gz"
```

- [ ] **Step 3: 更新存储循环中的变量引用**

存储循环内 `backend.PutObject(..., archive.Bytes())` 改为 `buf.Bytes()`。

- [ ] **Step 4: 验证**

Run: `go build ./...`
Run: `go test ./internal/discussion/ -v`
Run: `grep -n "os.Chdir" internal/discussion/discussion.go` → 无输出
Expected: build 通过、discussion 包测试 PASS、无 `os.Chdir`

- [ ] **Step 5: 提交**

```bash
git add internal/discussion/discussion.go
git commit -m "fix: archive discussions via absolute gitDir without os.Chdir"
```

---

### Task 6: 全仓最终验证

**Files:** 无改动（纯验证）。

- [ ] **Step 1: 全仓 `os.Chdir` 归零**

Run: `grep -rn "os.Chdir" internal/ cmd/`
Expected: 无输出

- [ ] **Step 2: 全量构建与测试**

Run: `go build ./...`
Run: `go test ./...`
Run: `go test -race ./internal/archive/ -v`
Expected: 全绿；`internal/archive` 的两个测试（条目名兼容 + 并发隔离）PASS

- [ ] **Step 3: 确认归档改走共享 helper**

Run: `grep -rn "FilesFromDisk" internal/`
Expected: 仅命中 `internal/archive/archive.go`（1 处）——说明三处调用点已无内联 `FilesFromDisk`。
Run: `grep -rn "archive.Create" internal/`
Expected: 命中 3 处：`internal/repository/repository.go`、`internal/issue/issue.go`、`internal/discussion/discussion.go`，调用均为 `archive.Create(syncCtx|ctx, gitDir, <target>)`。

（本任务无提交；改动已在 Task 1-5 各自提交。）
